syntax = "proto3";

package tknz.detail;

option go_package = "go.tknz.dev/internal/pb";

message Token {
	// Token
	bytes token = 1;

	// Token signature
	bytes signature = 2;

	// Key id used for the token signature
	string kid = 3;
}
